# Claude Code 작업 프롬프트

`claude` 실행 후 그대로 붙여넣어 쓰는 지시문.

---

## 0. 현재 상태 (2026-09-04 기준)

R4 재편집본(`videos/match_4set_new.mp4`)으로 작업 중.

**해결된 것**
- 두 선수 원본 해상도가 달라(1080×1920 vs 900×1600) 폰트 크기가 어긋나던 문제 → 편집에서 정규화, 템플릿 한 세트로 통일
- 좌표 재측정 완료 (P1/P2가 x만 다르고 w·h·y 동일하도록)
- P2 자리당 폭이 P1보다 1.6px 작던 문제 → P2 `w` 미세 조정
- f1의 1·3·5 템플릿 상호 간섭 → 같은 프레임에서 동시 추출로 해결

**남은 문제**
- P2의 1·2번곡이 8,09x,xxx 대에 갇힘 (실제 943만/993만)
- 원인 추정: 100~156초 구간에서 오인식이 통과한 뒤 단조증가 제약에 막힘

**실제 곡별 최종 점수 (검증 기준)**

| 곡 | P1 | P2 |
|---|---|---|
| 1 | 9,810,762 | 9,435,245 |
| 2 | 9,793,557 | 9,934,210 |
| 3 | 9,864,379 | 9,836,457 |
| 4 | 9,723,331 | 9,617,798 |

**곡 전환**: breaks `[156, 310, 480]`, freeze `[[146,166],[300,362],[470,490]]`

---

## 1. 남은 문제 해결 (지금 필요한 것)

```
R4 점수 인식에서 P2의 1·2번곡이 8,09x,xxx 대에 갇혀 있어.
실제로는 1번곡 9,435,245 / 2번곡 9,934,210 이야.

CLAUDE.md의 "9. 진단 절차"를 따라 원인을 찾아줘.

영상: videos/match_4set_new.mp4
설정: r4/config.json, r4/templates
breaks: r4/breaks.json
freeze: [[146,166],[300,362],[470,490]]

1. scan으로 P2가 어느 시각에서 멈추거나 급락하는지 찾기
2. 그 시점을 check --dump로 뽑아 크롭 이미지 확인
3. 크롭이 온전한데 인식이 틀리면 매칭 점수를 직접 출력해 원인 특정
4. 좌표 / 템플릿 / freeze 중 무엇을 고쳐야 하는지 판단
5. 수정 후 run 재실행, 위 표의 실제 점수와 대조

수정하기 전에 원인 분석 결과를 먼저 보여줘.
P1은 이미 오차 500~8,000 수준으로 잘 나오니 건드리지 말 것.
```

---

## 2. 새 영상으로 처음부터 세팅

```
새 영상으로 좌표와 템플릿을 처음부터 만들어줘.

영상: <경로>
출력: r4/config.json, r4/templates/, r4/match4.scores.json

작업 전 CLAUDE.md를 읽고 파이프라인과 함정을 파악할 것.

순서:
1. 여러 시점(40/150/300/450/570초) 프레임을 뽑아
   점수 위치가 전 구간 동일한지 먼저 확인
   - 다르면 편집 문제이므로 즉시 알려줄 것
2. 격자를 그려 좌표를 픽셀 단위로 실측
   - 두 선수 화면이 같은 배율이면 w/h/y는 같고 x만 달라야 정상
   - 크롭 후 자리당 폭을 비교해 1px 이상 차이나면 w 미세 조정
3. make_sheet로 시각별 점수를 확인해 batch_learn용 pairs 작성
4. 템플릿 생성 후 0~9를 개별 확대해 오염 검사
5. check로 최소 4개 시점 검증 (자릿수별 신뢰도 0.6 이상 목표)
6. 통과하면 run

각 단계 결과를 보여주고, 판단이 애매하면 물어봐.
곡 전환 시각과 실제 리절트 점수는 내가 알려줄게.
```

---

## 3. 템플릿 품질 점검

```
r4/templates 의 품질을 점검해줘.

f0, f1 각각 0~9를 개별 확대해서:
- 옆자리 침범
- 위아래 잘림
- 크기가 다른 숫자와 크게 차이 (같은 필드 안에서 ±20% 이상)
를 찾아 목록으로 알려줘.

문제가 있으면 그 숫자가 깨끗하게 나오는 시점을 make_sheet 결과에서
찾아 재추출하는 방법도 제안해줘.

주의: 3↔5↔8 처럼 형태가 비슷한 숫자는 하나만 고치면 다른 게 틀어진다.
문제되는 숫자들은 같은 프레임에서 동시에 추출할 것.
```

---

## 4. 좌표만 다시 잡기

```
영상을 재출력해서 좌표가 틀어졌어. r4/config.json의 ROI를 다시 잡아줘.

영상: <경로>

40/300/570초 프레임을 뽑아서:
- 각 선수의 field0(앞 4자리), field1(뒤 4자리) 영역을 격자로 실측
- 세 시점 모두에서 같은 좌표가 맞는지 교차 검증
- 크롭 후 자리당 폭이 P1/P2 간에 일치하는지 확인
- config 갱신 후 check로 검증

좌표가 시점마다 다르면 편집 문제이므로 알려줘.
```

---

## 5. 전체 재실행 및 검증

```
R4 전체를 다시 처리하고 검증해줘.

영상: videos/match_4set_new.mp4
breaks: [156, 310, 480]
freeze: [[146,166],[300,362],[470,490]]

실행 후:
1. 곡별 최종 점수를 아래 실제값과 대조해 오차 표로 보여줘
2. 곡 전환 지점(156/310/480초)에서 0으로 리셋되는지 확인
3. 곡 내부에 단조증가 위반이 없는지 확인
4. 구간별 최대 격차를 계산해 특정 구간만 비정상적으로 크지 않은지 확인

실제 점수:
  1번곡: P1 9,810,762 / P2 9,435,245
  2번곡: P1 9,793,557 / P2 9,934,210
  3번곡: P1 9,864,379 / P2 9,836,457
  4번곡: P1 9,723,331 / P2 9,617,798
```

---

## 6. 오버레이 수정

```
public/overlay.html 의 <대상>을 수정해줘.

요구사항: <내용>

주의:
- JS 수정 후 반드시 문법 검증
- R1/R3(4분할)과 R2(HUD 사용)에 영향 없는지 확인
- CSS는 골드/블랙 테마 유지
- 상시 애니메이션은 transform/opacity만 사용 (GPU 부하)
```

---

## 작업 시 참고

- **CLAUDE.md** — 파이프라인 상세, 실전 함정, 진단 절차
- **README.md** — 프로젝트 구조

**실행 환경** — 프로젝트 루트에서 `python tools/sdvx_score.py` 형태로 호출.

**PowerShell 따옴표** — `--player-templates` 같은 JSON 인자는 따옴표가 깨지므로 `--player-templates-file`로 파일을 넘길 것.

**전수 판독은 하지 말 것** — 12,000 프레임을 하나씩 눈으로 보는 건 비현실적이다. 템플릿 매칭이 본체이고, 문제 구간 몇 개만 골라 진단·수정하는 것이 역할.
