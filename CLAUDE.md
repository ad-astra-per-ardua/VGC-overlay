# 영상 점수 인식 시스템

리듬게임(SDVX) 대회 녹화본에서 화면에 표시된 점수를 프레임 단위로 읽어, 방송 오버레이가 재생 시각에 맞춰 점수를 표시할 수 있는 타임라인 JSON을 만든다.

---

## 1. 파이프라인

```
영상 파일
   ↓  ① 프레임 추출 (stride 간격으로 순차 디코딩)
   ↓  ② ROI 크롭 (config.json의 x/y/w/h)
   ↓  ③ 정규화 (밝은 행만 남기고 높이 32px로 리스케일)
   ↓  ④ 자릿수 분할 (폭을 digits 등분, 좌우 40% 여유)
   ↓  ⑤ 이진화 (Otsu)
   ↓  ⑥ 템플릿 매칭 (0~9 이미지와 비교)
   ↓  ⑦ 필드 합산 (field0 × 10000 + field1)
   ↓  ⑧ 후처리 (단조증가 제약, 곡 전환 리셋, 구간 무시)
타임라인 JSON
```

**핵심 상수**

| 상수 | 값 | 의미 |
|---|---|---|
| `CELL_H` | 32 | 모든 스트립·템플릿을 이 높이로 정규화 |
| `SEARCH_MARGIN` | 0.40 | 자릿수 검색 시 좌우 여유 비율 |
| `MATCH_THRESHOLD` | 0.35 | 이 값 미만이면 프레임 전체를 실패 처리 |

---

## 2. 설정 파일

```json
{
  "players": [
    {
      "fields": [
        { "x": 697, "y": 344, "w": 174, "h": 52,
          "digits": 4, "weight": 10000, "fixed_leading_zero": true },
        { "x": 861, "y": 354, "w": 108, "h": 42,
          "digits": 4, "weight": 1 }
      ]
    }
  ],
  "fps": 20,
  "max_score": 10000000
}
```

- **fields** — 점수가 화면에서 여러 덩어리로 나뉠 때 각각 정의. `weight`를 곱해 합산.
- **fixed_leading_zero** — 첫 자리를 매칭하지 않고 0으로 확정. 점수 상한이 정해져 첫 자리가 항상 0일 때.
- **fps** — 초당 샘플 수. `stride = round(영상fps / 이 값)`.

---

## 3. 템플릿

```
templates/
  f0/  0.png 1.png ... 9.png   (+ 0b.png 같은 변형 가능)
  f1/  0.png ... 9.png
```

- 파일명 첫 글자가 숫자면 그 숫자로 인식. 뒤에 알파벳을 붙여 **같은 숫자의 변형**을 여러 개 둘 수 있다.
- 로드 시 높이를 `CELL_H`로 리스케일하므로 원본 크기는 자유.
- **필드마다 폰트 크기가 다르므로 f0/f1을 따로 만든다.**

---

## 4. 매칭 보정

`match_cell()`은 단순 최고점 채택이 아니다.

- **offset penalty (0.5)** — 매칭 위치가 검색창 중앙에서 벗어날수록 감점
- **"1" penalty (0.15)** — 숫자 1은 가는 세로획이라 다른 숫자 테두리에 우연히 잘 맞음

보정은 **순위 결정에만** 쓰고, 반환 신뢰도는 보정 전 원점수다.

⚠️ **이 보정이 역효과를 낼 수 있다.** 좌표가 정확해진 상태에서는 offset penalty가 정답을 떨어뜨리는 경우가 관측됐다. 특정 숫자가 계속 틀리면 이 값을 의심할 것.

---

## 5. 후처리 (`postprocess`)

점수는 한 곡 안에서 단조증가한다는 제약으로 오인식을 거른다.

- 값이 직전보다 작아지거나 `max_score` 초과 → 직전 값 유지
- 급격한 상승(`max_rate_per_sec` 초과) → 보류. 단 `last == 0`이면 예외
- **breakpoints** — 이 시각에 `last = 0`으로 리셋 (다음 곡 시작)
- **freezes** `[[시작,끝], ...]` — 구간 전체를 무시하고 직전 값 유지

⚠️ **단조증가 제약의 함정** — 오인식으로 실제보다 높은 값이 한 번 통과하면, 그 뒤 정상값이 전부 거부되어 곡 내내 그 값에 갇힌다. 곡별 최종 점수가 실제보다 크게 낮으면 이 패턴을 의심할 것.

---

## 6. 서브커맨드

| 명령 | 용도 |
|---|---|
| `frame` | 특정 시각 프레임을 PNG로 저장 (좌표 실측용) |
| `learn` | 라벨링된 프레임에서 템플릿 추출 |
| `run` | 전체 처리 → 타임라인 JSON |
| `check` | 한 시점 인식 결과 + 자릿수별 신뢰도 (`--dump`로 크롭 저장) |
| `scan` | 구간을 훑어 어디서 끊기는지 표로 확인 |

**주요 옵션**
- `--templates` — 모든 선수가 같은 템플릿 사용
- `--player-templates-file` — 선수별 템플릿 (JSON 배열 파일)
- `--breaks-file`, `--freeze` — 곡 경계 처리
- `--threshold` — 인식 임계값 (기본 0.35)
- `--max-rate` — 초당 최대 상승폭 (기본 5000000)

---

## 7. 실전 함정

작업 중 실제로 겪은 것들. 새 영상에서도 재발할 수 있다.

### 7.1 원본 해상도 차이 (가장 큰 함정)

선수마다 녹화 해상도가 다르면(1080×1920 vs 900×1600) 편집 시 배율이 달라지고, **폰트 픽셀 크기가 어긋나 템플릿이 안 맞는다.**

증상: 한 선수만 인식률이 낮고, 템플릿을 고쳐도 계속 다른 숫자가 틀림.

**근본 해결은 편집 단계에서 두 소스를 같은 크기로 정규화하는 것.** 프리미어에서 각 클립의 비율 조정으로 최종 픽셀 크기를 맞춘다. 이걸 하면 템플릿 한 세트로 통일 가능.

**정규화해도 1~2px 차이는 남는다.** 크롭 후 `자리당 폭`을 비교해서(예: P1 31.8px vs P2 30.2px) 차이가 있으면 좌표의 `w`를 미세 조정한다.

### 7.2 클립 누락·설정 불일치

편집에서 클립이 여러 개로 나뉘면 **일부에만 비율/위치가 적용**되는 경우가 있다. 시점마다 점수 위치가 달라진다.

확인: 여러 시점(40/150/300/450/570초) 프레임을 뽑아 점수 위치가 동일한지 대조.

### 7.3 자릿수 경계 어긋남

등분 방식이라 ROI 폭이 실제 글자 범위와 어긋나면 경계가 글자 중간을 자른다. 반쪽만 남은 숫자는 엉뚱한 값으로 인식된다.

진단: strip에서 밝은 열 분포를 구해 실제 글자 구간을 찾고, 등분 경계와 비교.

```python
cols = (strip > 110).sum(axis=0)
# 연속 구간을 찾아 4등분 경계와 대조
```

### 7.4 템플릿 상호 간섭

한 숫자 템플릿을 고치면 다른 숫자가 틀어지는 현상. 3↔5↔8처럼 형태가 비슷한 것끼리 서로 밀어낸다.

**해결: 문제되는 숫자들을 같은 프레임에서 동시에 추출한다.** 따로따로 다른 시점에서 뽑으면 크기·밝기가 미묘하게 달라 계속 반복된다.

### 7.5 흐린 리딩 제로

게임 UI가 앞자리 0을 회색으로 표시. 밝은 숫자 템플릿과 매칭이 안 되므로 `0b.png` 변형을 별도로 둔다.

### 7.6 곡 전환 구간 오인식

페이드 인/아웃 중 노이즈가 임계값을 넘어 통과하면 단조증가 제약 때문에 그 값이 곡 내내 유지된다. `--freeze`로 해당 구간을 무시할 것.

### 7.7 batch_learn의 한계

자동 추출은 어느 프레임을 골랐는지에 결과가 좌우된다. 옆자리가 침범하거나 잘린 템플릿이 조용히 만들어진다.

**생성 후 반드시 0~9를 개별 확대해서 눈으로 확인할 것.** 크기가 다른 것과 크게 다르면 오염 신호다.

이미 존재하는 숫자는 건너뛰므로, 특정 숫자만 다시 뽑으려면 **해당 파일을 삭제한 뒤** 실행하거나 `--overwrite`를 쓴다. 삭제가 실제로 됐는지 `Test-Path`로 확인할 것.

### 7.8 템플릿 크롭 시 여백

위아래 여백을 많이 포함하면 32px 정규화 시 글자가 작게 축소되어 실제 화면과 크기가 안 맞는다. **밝은 행만 남기고 타이트하게 자를 것.**

아래쪽 EX SCORE 바 같은 다른 UI가 섞이지 않도록 y 범위도 주의.

---

## 8. 검증 기준

곡별 최종 점수가 게임 리절트 화면과 일치해야 한다. 오차가 있어도 **팀 간 격차를 뒤집지 않으면** 방송용으로 허용.

추가 검증:
- 곡 전환 지점에서 0으로 리셋되는가
- 곡 내부에 단조증가 위반이 없는가
- 격차가 특정 구간만 비정상적으로 크지 않은가 (미터 스케일이 망가짐)

---

## 9. 진단 절차

문제가 생겼을 때 이 순서로 좁혀나간다.

**1단계 — 어디가 문제인지**
```bash
scan --start 0 --end 156 --step 10
```
값이 멈추거나 급락하는 시각을 찾는다.

**2단계 — 크롭이 맞는지**
```bash
check --at <시각> --dump <폴더>
```
크롭 이미지를 열어 실제 숫자가 온전히 담겼는지 본다.
- 잘렸다 → 좌표 문제 (3단계)
- 온전하다 → 템플릿 문제 (4단계)

**3단계 — 좌표 실측**
원본 프레임에 격자를 그려 픽셀 단위로 잰다. 밝기 스캔은 배경 이펙트에 속으니 보조로만.

**4단계 — 템플릿 점검**
매칭 점수를 직접 출력해 정답이 몇 위인지 본다. 정답이 후보에도 없으면 그 템플릿을 재추출, 근소하게 밀리면 보정 파라미터를 의심.

**5단계 — 후처리 확인**
`check`는 맞는데 `run`이 틀리면 postprocess 문제다. 단조증가 제약에 갇혔는지, freeze 구간이 맞는지 본다.